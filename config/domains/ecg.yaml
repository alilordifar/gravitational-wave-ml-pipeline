domain: ecg

# --- source dataset (placeholder — no connector built yet) ---
lead: II
patient_id: "42"
sample_rate_hz: 250
duration_sec: 600

# --- windowing ---
window_duration_sec: 2
samples_per_window: 500   # sample_rate_hz * window_duration_sec

# --- storage ---
s3_raw_prefix: raw/ecg